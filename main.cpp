#include "ProbingHashTable.h"
#include "DoubleHashTable.h"
#include "ChainingHashTable.h"
#include "StopWatch.h"

int main()
{
	ProbingHashTable P;
	DoubleHashTable D;
	ChainingHashTable C;

	//P.printAll("dictionary.txt");
	//D.printAll("dictionary.txt");
	//C.printAll("dictionary.txt");
	/*
	t.start();
	for (int i = 0; i < 3000; i++) {
		if (i % 100 == 0) {				//every multiple of 100
			cout << t.ms() << endl;		//output the time taken since we started
		}
		U.insertItem(1);				//we insert 100 before the if statement is tested
	}
	t.stop();
	*/
}